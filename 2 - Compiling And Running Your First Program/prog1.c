#include <stdio.h>  //allows for functions such as printf, almost always needed for a C program

int main (void) //name of program is main and returns int. main is the name of a function that takes no arguments (void). 
{
    printf("Programming is fun\n");

    return 0;    //announce that program is finished. 0 is used by convention to indicate that there were no ers
}
